# Writes the JCL inline, submits it, and prints the spool output.
- name: Hello World JCL from local job
  hosts: zoslab
  gather_facts: false
  collections:
    - ibm.ibm_zos_core
  environment: "{{ environment_vars }}"

  tasks:
  - name: Convert local JCL to IBM-037 and submit the job.
    zos_job_submit:
      src: "{{ playbook_dir }}/JCL/hello_world.jcl"
      location: LOCAL
      encoding:
        from: ISO8859-1
        to: IBM-037
    register: job_output

  - name: Show job ID and return code
    debug:
      var: job_output.jobs[0].job_id